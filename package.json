{
  "name": "meridian-logistics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "vercel-build": "pnpm install --frozen-lockfile && pnpm run build"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "pnpm": "^9.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
